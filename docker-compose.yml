version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bytebank-app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: always

# Futuramente, você poderá adicionar um serviço de banco de dados aqui:
# db:
#   image: postgres:15
#   ...